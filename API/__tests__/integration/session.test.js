// describe('Authentication', () => {
//   it('should receive JWT token when authenticated with valid credencials', () => {
//     // asdasd
//   });
// });

// describe('Register', () => {
//   it('should receive JWT token when authenticated with valid credencials', () => {
//     // asdasd
//   });
// });

describe('Example', () => {
  it('should sum two numbers', () => {
    const x = 2;
    const y = 4;

    const sum = x + y;

    expect(sum).toBe(6);
  });
});
